## 本地预览

```bash
quarto preview
```

## 本地渲染

```bash
quarto render
```

渲染产物会输出到 `docs/` 目录，该目录需要提交到仓库。

## 推送到 GitHub

```bash
git add .
git commit -m "Update blog content"
git push origin main
```
